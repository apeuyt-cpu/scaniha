import Dashboard from '@/components/admin/dashboard/Dashboard'

export const dynamic = 'force-dynamic'

/** Admin home — live, plan-aware dashboard (v2 remake; replaced the tile hub). */
export default function AdminHome() {
  return <Dashboard />
}
