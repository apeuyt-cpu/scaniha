'use client'

// The app is TND-only. The currency selector is intentionally disabled
// (renders nothing) so it can stay imported across the app without effect.
export default function CurrencySelector(_props: { showLabel?: boolean }) {
  return null
}
