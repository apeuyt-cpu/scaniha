'use client'

// The app is French-only. The language switcher is intentionally disabled
// (renders nothing) so it can stay imported across the app without effect.
export default function LanguageSwitcher(_props: { showLabel?: boolean }) {
  return null
}
